// I've already set up Jest in this project, but I haven't had time to implement it yet.
// I'm more familiar with Cypress(E2E), so I apologize for not getting to the Jest tests.


describe('Basic Jest Test', () => {
  test('should add two numbers correctly', () => {
    const result = 2 + 2;
    expect(result).toBe(4);
  });
});
